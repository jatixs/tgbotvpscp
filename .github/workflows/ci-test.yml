# .github/workflows/ci-test.yml

name: CI Тест (Линтинг и Полный Скан Безопасности)

# Запускаем этот воркфлоу при push или pull request в основные ветки
on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    name: 🧪 Тестирование кода
    runs-on: ubuntu-latest

    steps:
      # 1. Получаем код из репозитория
      - name: ⬇️ Получение кода
        uses: actions/checkout@v4

      # 2. Настраиваем Python 3.10
      - name: 🐍 Настройка Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 3. Устанавливаем зависимости
      - name: 📦 Установка зависимостей
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          
          # Устанавливаем зависимости проекта
          pip install -r requirements.txt
          
          # Устанавливаем инструменты для CI
          pip install flake8 bandit

      # 4. Запускаем Flake8 (линтер)
      - name: 🎨 Запуск Линтера (Flake8)
        run: |
          source venv/bin/activate
          echo "🎨 Проверка стиля кода (Flake8) для *.py файлов..."
          # Flake8 проверяет только Python-файлы
          flake8 bot.py watchdog.py --count --ignore=E501 --show-source --statistics

      # 5. Запускаем Bandit (полный сканер безопасности)
      - name: 🛡️ Запуск Сканера Безопасности (Bandit) - ПОЛНЫЙ СКАН
        run: |
          source venv/bin/activate
          echo "🛡️ Поиск уязвимостей (Bandit) - полный рекурсивный скан..."
          
          # -r . : рекурсивно сканировать все файлы в директории
          # -x ./venv : исключить директорию с виртуальным окружением
          # (Мы убрали -ll, чтобы видеть ВСЕ уровни уязвимостей)
          bandit -r . -x ./venv